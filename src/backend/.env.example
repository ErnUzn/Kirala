# Kirala - Örnek Ortam Değişkenleri Dosyası
# Bu dosyayı .env olarak kopyalayın ve değerleri uygun şekilde güncelleyin

# Uygulama Ayarları
NODE_ENV=development
PORT=3000

# Veritabanı Ayarları
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kirala
DB_USER=postgres
DB_PASSWORD=your_password
DB_SSL=false

# Alternatif olarak tam bağlantı URL'si kullanabilirsiniz
# DATABASE_URL=postgresql://postgres:your_password@localhost:5432/kirala

# JWT Ayarları
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=7d

# CORS Ayarları
CORS_ORIGIN=http://localhost:3000

# Dosya Yükleme Ayarları
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880  # 5MB

# Email Ayarları
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=your_smtp_password
EMAIL_FROM=Kirala <info@kirala.com>

# Ödeme Entegrasyonu
PAYMENT_PROVIDER=stripe  # stripe, iyzico vb.
STRIPE_SECRET_KEY=your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

# Loglama
LOG_LEVEL=info  # error, warn, info, verbose, debug, silly 